make an an arraylist with an arraylist in the bulletinboard impl

this set should contain a pair that is given to 


get(i, b): Let t = B(b).
met B de hash van
If 〈v, t〉 ∈ B[i] for some value v, return
v and remove 〈v, t〉 from B[i]. Otherwise return ⊥, and
leave B[i] unchanged.